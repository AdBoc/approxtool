syntax = "proto3";
package approximationService;

message CalculationRequest {
  string model = 1;
  string type = 2;
}

message CalculationResult {
  string result = 1;
}

service ApproximationService {
  rpc GetApproximation(CalculationRequest) returns (CalculationResult);
}