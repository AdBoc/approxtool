services:
  # protobuff:
  #   build:
  #     context: ./protos
  #     dockerfile: dockerfile
  envoy:
    build:
      context: ./envoy
      dockerfile: dockerfile
    ports:
      - "8080:8080"
      - "9901:9901"
  usersrv:
    build:
      context: ./server/usersrv
      dockerfile: dockerfile
    ports:
      - "9000:9000"
    depends_on:
      - envoy
      # - protobuff
  modelsrv:
    build:
      context: ./server/modelsrv
      dockerfile: dockerfile
    ports: 
      - "9001:9001"
    depends_on:
      - envoy
      # - protobuff
  approxsrv:
    build:
      context: ./server/approxsrv
      dockerfile: dockerfile
    ports:
      - "9002:9002"
    depends_on:
      - envoy
      # - protobuff
  client:
    build:
      context: ./client
      dockerfile: dockerfile
    ports:
      - "3000:3000"
    # depends_on:
    #   - protobuff
